#!/usr/bin/env bash

masked=$(bspc query -N -n .hidden -d focused)

if [ -z "$masked" ]; then
	bspc node focused -g hidden=on
else
        IFS=';' read -ra my_array <<< "$masked"
        for i in "${my_array[@]}"
        do
          bspc node "$i" -g hidden=off
        done
fi
